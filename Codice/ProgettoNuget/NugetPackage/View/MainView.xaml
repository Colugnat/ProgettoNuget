<UserControl x:Class="NugetPackage.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NugetPackage.View"
             xmlns:viewmodels="clr-namespace:NugetPackage.ViewModel"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" Height="650" Width="725">
    <UserControl.DataContext>
        <viewmodels:NugetViewModel></viewmodels:NugetViewModel>
    </UserControl.DataContext>
    <Grid>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding Path=SearchNewsCommand}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="65*"/>
            <RowDefinition Height="30*"/>
        </Grid.RowDefinitions>
        <TextBox Height="22" FontSize="15" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Width="620" Name="path" Text="{Binding Path=Directory}" Grid.ColumnSpan="2" Margin="0,18,0,19" BorderBrush="{x:Null}"/>
        <Button FontSize="10" Content="Browse..." Name="browse" HorizontalAlignment="Right" VerticalAlignment="Center" Width="100" Command="{Binding Path=BrowseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Grid.Column="1" Height="20" Background="#FFA6A6A6" BorderBrush="{x:Null}" Foreground="Black" Margin="0,19,0,20"/>
        <TextBlock Text="Search: " FontSize="18" Name="search" HorizontalAlignment="Left" VerticalAlignment="Center" Width="60" Height="24" Grid.Row="1" Margin="70,17,0,18"/>
        <TextBox FontSize="15" HorizontalAlignment="Center" Text="{Binding Path=StartSearch, UpdateSourceTrigger=PropertyChanged}" Height="22" VerticalAlignment="Center" Width="430" Name="input" Grid.Row="1" Grid.ColumnSpan="2" Margin="148,19,147,18" BorderBrush="{x:Null}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="KeyUp">
                    <i:InvokeCommandAction Command="{Binding Path=SearchCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </TextBox>
        <ListBox FontSize="15" Name="listNuget" ItemsSource="{Binding Path=ResultSearch}" SelectedItem="{Binding Path=NamePackage}" Grid.Row="2">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseLeftButtonUp">
                    <i:InvokeCommandAction Command="{Binding Path=ShowCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListBox>
        <TextBox FontSize="15" Name="version" IsReadOnly="True" Margin="10,0,0,20" Text="{Binding Path=ResultPackage, Mode=OneWay}" Grid.Row="2" Grid.Column="1" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderBrush="{x:Null}"/>
        <Button Content="Save" FontSize="10" Name="save" Margin="10,0,0,0" VerticalAlignment="Bottom" Command="{Binding Path=SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Grid.Row="2" Grid.Column="1" Height="20" Background="#FFA6A6A6" BorderBrush="{x:Null}" Foreground="Black"/>
        <TextBox FontSize="15" Name="logArea" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,5,300,5" IsReadOnly="True" Text="{Binding Path=ResultLog, Mode=OneWay}" TextWrapping="Wrap" VerticalScrollBarVisibility="Hidden" TextChanged="logArea_TextChanged" IsEnabled="False" BorderBrush="{x:Null}"/>
        <TextBlock Text="News: " FontSize="18" Name="news" HorizontalAlignment="Left" VerticalAlignment="Top" Width="60" Height="24" Grid.Row="3" Grid.Column="1" Margin="70, 10, 0, 0"/>
        <ListBox FontSize="15" Name="listNews" ItemsSource="{Binding Path=ResultSearchNews}" SelectedItem="{Binding Path=NamePackage}" Grid.Row="3" Grid.Column="1" Margin="70, 40, 0, 5">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseLeftButtonUp">
                    <i:InvokeCommandAction Command="{Binding Path=ShowCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListBox>
    </Grid>
</UserControl>